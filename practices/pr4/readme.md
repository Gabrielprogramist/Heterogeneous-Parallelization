# Practical Work №4

**Оптимизация параллельного кода на GPU с использованием различных типов памяти CUDA**

---

## Общее описание работы

Данная практическая работа посвящена изучению различных типов памяти в CUDA
(**глобальной, разделяемой и локальной**) и их влияния на производительность параллельных вычислений на GPU.

В рамках работы были реализованы и исследованы:

* параллельная редукция суммы массива;
* сортировка данных на GPU;
* измерение производительности для разных размеров входных данных.

Работа выполнена на GPU **NVIDIA Tesla T4 (compute capability 7.5)** в среде **Google Colab**.

---

## Логическое разбиение работы на файлы

Для удобства понимания, отладки и соответствия структуре задания, программа была **разделена на несколько независимых CUDA-файлов**, каждый из которых решает отдельную подзадачу.

Такое разбиение позволяет:

* чётко связать код с пунктами задания;
* изолировать разные типы памяти;
* упростить анализ производительности и объяснение результатов.

---

## Описание файлов проекта

### 1. `task1_data_generation.cu`

**Назначение:**

* генерация массива случайных чисел заданного размера (1 000 000 элементов);
* копирование данных в глобальную память GPU.

**Роль в работе:**

* подготовка входных данных для всех последующих вычислений;
* демонстрация работы с глобальной памятью.

---

### 2. `task2_reduce_global.cu`

**Назначение:**

* реализация параллельной редукции суммы массива;
* используется **только глобальная память** и `atomicAdd`.

**Роль в работе:**

* наивный (базовый) вариант редукции;
* используется как эталон для сравнения производительности.

---

### 3. `task2_reduce_shared.cu`

**Назначение:**

* редукция суммы массива с использованием **глобальной и разделяемой памяти**;
* промежуточная редукция выполняется внутри блока в `shared memory`.

**Роль в работе:**

* демонстрация оптимизации за счёт уменьшения обращений к глобальной памяти;
* сравнение с вариантом `task2_reduce_global.cu`.

---

### 4. `task3_bubble_sort_local.cu`

**Назначение:**

* сортировка небольших подмассивов методом пузырька;
* данные подмассива копируются в **локальную память потока**.

**Роль в работе:**

* демонстрация использования локальной памяти (регистров);
* подготовка отсортированных подмассивов для последующего слияния.

---

### 5. `task3_merge_shared.cu`

**Назначение:**

* слияние двух соседних отсортированных подмассивов;
* использование **разделяемой памяти** для буферизации данных.

**Роль в работе:**

* демонстрация ускорения операций слияния за счёт shared memory;
* один проход алгоритма merge (width + width → 2·width).

---

### 6. `task4_performance_measure.cu`

**Назначение:**

* измерение времени выполнения всех основных алгоритмов;
* тестирование для размеров массивов:

  * 10 000
  * 100 000
  * 1 000 000

**Измеряемые алгоритмы:**

* редукция (только глобальная память);
* редукция (глобальная + разделяемая память);
* пузырьковая сортировка подмассивов (локальная память);
* слияние подмассивов (разделяемая память).

**Результат:**

* вывод таблицы времени выполнения;
* сохранение результатов в файл `performance.csv` для построения графиков.

---

## Дополнительные материалы

### 📁 Папка `block_diagrams`

В данной папке находятся **блок-схемы алгоритмов**, использованных в работе:

* редукция суммы;
* пузырьковая сортировка подмассивов;
* слияние отсортированных подмассивов.

Блок-схемы отражают:

* логику работы алгоритмов;
* распределение вычислений между потоками;
* использование различных типов памяти CUDA.

---

### 📄 Файл `outputs_answer.pdf`

В данном файле содержатся:

* **выводы (outputs)** всех программ при запуске;
* **результаты измерения производительности**;
* **ответы на контрольные вопросы**, приведённые в конце задания.

Файл используется как итоговый отчёт по практической работе.

---

## Итог

В ходе выполнения практической работы:

* были реализованы основные алгоритмы параллельной обработки данных на GPU;
* на практике изучено влияние различных типов памяти CUDA;
* показано, что использование разделяемой памяти существенно повышает производительность;
* получены экспериментальные данные и оформлены результаты.

Работа полностью соответствует поставленному заданию и может использоваться для защиты практической работы.

