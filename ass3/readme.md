## **Assignment 3**
**GPU Architecture and CUDA Optimization**

---

### Общее описание

Данный ассаймент посвящён изучению архитектуры графических процессоров и принципов оптимизации программ, написанных с использованием технологии **CUDA**.

Основной целью работы является экспериментальное исследование влияния:

* типов памяти GPU,
* конфигурации сетки и блоков потоков,
* шаблонов доступа к глобальной памяти,
  на производительность CUDA-программ.

Все эксперименты выполнялись на GPU **NVIDIA Tesla T4 (compute capability 7.5)** в среде **Google Colab**.

---

### Логическое разбиение работы

Ассаймент был логически разделён на несколько независимых задач.
Каждая задача реализована в отдельном CUDA-файле, что упрощает:

* анализ результатов,
* сопоставление с условиями задания,
* повторное использование и модификацию кода.

---

### Структура проекта

```text
Assignment3/
│
├── task1_global_vs_shared.cu
├── task2_block_size_effect.cu
├── task3_coalesced_vs_uncoalesced.cu
├── task4_optimized_config.cu
│
├── block_diagrams/
│   ├── task1_memory_access.png
│   ├── task2_block_config.png
│   ├── task3_memory_coalescing.png
│   └── task4_optimization_flow.png
│
├── outputs_answer.pdf
└── README.txt
```

---

### Описание задач и файлов

---

### `task1_global_vs_shared.cu`

**Назначение:**
Сравнение производительности использования глобальной и разделяемой памяти GPU.

**Содержание:**

* реализовано ядро, использующее только глобальную память;
* реализовано ядро с буферизацией данных в shared memory;
* измеряется и сравнивается время выполнения.

**Цель:**
Показать, что использование shared memory может значительно сократить время выполнения при повторном использовании данных.

---

### `task2_block_size_effect.cu`

**Назначение:**
Исследование влияния размера блока потоков на производительность CUDA-ядра.

**Содержание:**

* выполняется сложение двух массивов одинакового размера;
* запуск производится с различными размерами блоков (128, 256, 512);
* измеряется время выполнения для каждого варианта.

**Цель:**
Продемонстрировать, что неправильный выбор размера блока может приводить к снижению производительности.

---

### `task3_coalesced_vs_uncoalesced.cu`

**Назначение:**
Сравнение коалесцированного и некоалесцированного доступа к глобальной памяти.

**Содержание:**

* реализованы два ядра с разными шаблонами доступа к памяти;
* измеряется время выполнения каждого ядра.

**Цель:**
Показать, насколько сильно шаблон доступа к памяти влияет на пропускную способность и общее время выполнения.

---

### `task4_optimized_config.cu`

**Назначение:**
Подбор оптимальной конфигурации сетки и блоков потоков.

**Содержание:**

* используется ядро сложения векторов;
* перебираются различные размеры блоков потоков;
* измеряется среднее время выполнения;
* определяется оптимальная и заведомо неэффективная конфигурации;
* рассчитывается ускорение.

**Цель:**
На практике показать процесс оптимизации CUDA-программы и важность подбора параметров запуска.

---

### Дополнительные материалы

#### 📁 Папка `block_diagrams`

Содержит блок-схемы, иллюстрирующие:

* структуру CUDA-ядер;
* потоки данных между памятью и вычислениями;
* различия в шаблонах доступа к памяти;
* процесс выбора оптимальной конфигурации.

---

#### 📄 Файл `outputs_answer.pdf`

Включает:

* выводы всех программ при запуске;
* таблицы с измерениями времени выполнения;
* результаты сравнений и ускорений;
* подробные ответы на контрольные вопросы ассаймента.

---

### Итог

В ходе выполнения Assignment 3:

* изучены основные типы памяти CUDA и их влияние на производительность;
* показана важность коалесцированного доступа к глобальной памяти;
* экспериментально подтверждено влияние размера блока потоков;
* выполнена оптимизация конфигурации запуска CUDA-ядер.

Полученные результаты демонстрируют, что эффективная работа с памятью и правильная настройка параметров запуска являются ключевыми факторами производительности CUDA-программ.
