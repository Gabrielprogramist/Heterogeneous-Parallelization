
## **Практическая работа №5**
**Реализация параллельных структур данных на GPU**

---

### Общее описание

Данная практическая работа посвящена реализации и исследованию **параллельных структур данных** на графическом процессоре с использованием технологии **CUDA**.

В рамках работы были реализованы:

* параллельный стек (LIFO);
* параллельная очередь (FIFO);
* экспериментальное сравнение их производительности.

Все реализации используют **атомарные операции CUDA** для обеспечения корректного доступа к данным из множества потоков.

Вычисления выполнялись на GPU **NVIDIA Tesla T4 (compute capability 7.5)** в среде **Google Colab**.

---

### Логическое разбиение работы на файлы

Работа была логически разделена на несколько независимых CUDA-файлов, каждый из которых соответствует отдельному пункту задания.
Такой подход упрощает:

* анализ кода;
* отладку;
* сопоставление с требованиями практической работы.

---

### Описание файлов проекта

---

### `task1_parallel_stack.cu`

**Назначение:**

* реализация параллельного стека (LIFO);
* поддержка операций `push` и `pop`;
* использование атомарных операций (`atomicAdd`, `atomicSub`).

**Особенности реализации:**

* стек хранится в глобальной памяти GPU;
* переменная `top` обновляется атомарно;
* операции выполняются параллельно из нескольких потоков.

---

### `task2_parallel_queue.cu`

**Назначение:**

* реализация параллельной очереди (FIFO);
* поддержка операций `enqueue` и `dequeue`;
* безопасный параллельный доступ.

**Особенности реализации:**

* используются атомарные указатели `head` и `tail`;
* элементы добавляются в конец очереди и извлекаются из начала;
* очередь хранится в глобальной памяти GPU.

---

### `task3_compare_performance.cu`

**Назначение:**

* сравнение производительности параллельного стека и очереди;
* измерение времени выполнения операций на GPU.

**Методика измерений:**

* каждая структура данных обрабатывается в две фазы:

  1. добавление элементов (push / enqueue);
  2. удаление элементов (pop / dequeue);
* замер времени производится с использованием `cudaEvent`.

**Результаты:**

* выводится время выполнения операций;
* рассчитывается пропускная способность (операций в миллисекунду);
* проверяется корректность выполнения по числу успешных операций.

---

### Дополнительные материалы

#### 📁 Папка `block_diagrams`

Содержит блок-схемы:

* параллельного стека;
* параллельной очереди.

Блок-схемы иллюстрируют:

* логику выполнения операций;
* использование атомарных операций;
* взаимодействие потоков с общей памятью.

---

#### 📄 Файл `outputs_answer.pdf`

В данном файле представлены:

* выводы программ при запуске всех трёх задач;
* результаты измерения производительности;
* ответы на контрольные вопросы из задания.

Файл используется как итоговый отчёт по практической работе.

---

### Итог

В ходе выполнения практической работы:

* были реализованы основные параллельные структуры данных на GPU;
* изучены проблемы синхронизации при параллельном доступе;
* показана роль атомарных операций в обеспечении корректности;
* выполнено экспериментальное сравнение производительности стека и очереди.
